# 🗃 ŌTOMO · 产品修改建议 & 设计决策留档册

> **用途**：独立留存用户/产品侧提出的所有修改建议与设计决策。与 `decisions.md`（内部技术决策 / Changelog）分离，确保每一条产品建议都**原文可追溯**。
> **规则**：只增不减；每条建议独立条目；按时间倒序；原话用 `> 引用块` 保留。
> **索引格式**：`A01 / A02 / A03 …`（A 代表 Advisory / 建议）

---

## A01 · 插入审核环节 + 三轮补搜机制

- **提出时间**：2026-04-22 22:23
- **提出人**：Ethan
- **状态**：✅ 已采纳，落地为 v2.3
- **相关产出**：
  - `docs/reviewer-sop.md`（审核官完整 SOP）
  - `docs/decisions.md` §2.12 + Changelog v2.3
  - `tools/online_refresh.py` 审核循环框架
  - `data/outfit_archive.json` meta.review 字段
  - `index.html` 顶部"本期图片数量有限"banner

### 原始需求（原文保留）

> 在原流程的 Step 2 和 Step 3 之间，插入审核环节：
> Step 2  搜索候选图片（搜索 Agent）
>   ↓
> Step 2.5  审核官逐张审核，输出 PASS/REJECT
>   ↓
> Step 2.6  不足则打回补搜，最多循环3轮
>   ↓
> Step 3  去重过滤（只对 PASS 的图片执行）
>   ↓
> Step 4  渲染瀑布流
>
> # 角色：穿搭图片审核官
>
> 你是一个专业的男装穿搭图片质量审核员。
> 你的唯一职责是：对搜索 Agent 提交的候选图片逐一审核，
> 判断每张图片是否符合发布标准，并给出明确的通过/驳回结论。
>
> ---
>
> ## 审核标准（所有条件必须同时满足才能通过）
>
> ### ✅ 通过条件
> 1. **人物完整性**：图片中必须有真实人物出镜，且能看到至少从头到膝盖的完整身形（全身或四分之三身）
> 2. **性别**：着装者为男性
> 3. **穿搭完整性**：可清晰识别至少上下身2件以上单品的搭配组合
> 4. **图片质量**：画面清晰，主体无严重遮挡（遮挡面积不超过20%）
> 5. **内容相关性**：图片主体是人物穿搭，而非产品平铺、静物、活动现场或建筑背景
>
> ### ❌ 驳回条件（满足任意一条即驳回）
> - 仅展示单件商品的平铺图、挂拍图、模特局部图
> - 仅展示鞋子、配件、包袋、饰品等局部特写
> - 着装者为女性或性别模糊
> - 图片主体为非穿搭内容（静物、建筑、活动现场等）
> - 画质模糊、严重过曝、大面积遮挡
> - 图片年份早于2021年（如可判断）
>
> ---
>
> ## 审核流程
>
> **输入**：搜索 Agent 提交的候选图片列表（含图片URL、来源类型、品牌/活动名）
>
> **逐张执行以下步骤：**
>
> Step 1｜读取图片
> 加载图片URL，对图片内容进行视觉理解。
>
> Step 2｜逐项核查
> 对照上方通过条件和驳回条件逐项判断，记录每项的判断结果。
>
> Step 3｜给出结论
> - 输出 `PASS` 或 `REJECT`
> - 若 `REJECT`，注明主要驳回原因（一句话即可，如："仅为鞋子局部特写"）
>
> Step 4｜汇总报告
> 所有图片审核完毕后，输出汇总结果：
> - 通过数量 / 提交总数
> - 驳回图片列表及原因
> - 是否需要补充（若通过数量 < 目标数量，注明需补充几张）
>
> ---
>
> ## 输出格式
>
> ```json
> {
>   "total_submitted": 18,
>   "total_passed": 13,
>   "total_rejected": 5,
>   "need_supplement": 2,
>   "results": [
>     { "image_url": "https://...", "verdict": "PASS" },
>     { "image_url": "https://...", "verdict": "REJECT", "reason": "仅为鞋子局部特写，无完整人物穿搭" }
>   ]
> }
> ```
>
> ---
>
> ## 补充机制
>
> 若汇总后通过数量不足目标数量（12–18张），
> 立即将 `need_supplement` 数量反馈给搜索 Agent，
> 搜索 Agent 需重新搜索补充，补充结果同样须经过本审核官二次审核，
> 循环执行，直至通过数量达标为止。
> 最多循环 **3轮**，若3轮后仍不达标，以实际通过数量发布，并在页面顶部注明"本期图片数量有限"。
>
> 产品的修改建议需要留档。请保留所有设计决策、修改建议，都单独记录。

### 落地解读（Coach/PM 侧翻译）

| 需求要点 | 落地决策 |
|---|---|
| 审核环节独立 | 在 Step 2 与 Step 3 之间新增 Step 2.5（审核）+ Step 2.6（补搜循环） |
| 通过 5 条 / 驳回 6 条标准 | 原文搬入 `reviewer-sop.md` §3，一字不改 |
| JSON 输出契约 | 原文保留，新增 `round` / `target_count` 两个字段以支持循环 |
| 最多 3 轮 | 硬编码于 `online_refresh.py::REVIEW_MAX_ROUNDS = 3` |
| 兜底文案 | 前端 `#review-banner`："⚠️ 本期图片数量有限（actual/target），下一期将补足。" |
| 留档要求 | 建立本文件（change-log-suggestions.md）；每次产品建议 A## 独立条目原文保留 |

### 实现分期

- **Phase 1（本次 v2.3）**：流程框架 + JSON 契约 + 循环逻辑 + 落盘字段 + 前端 banner + 规则兜底审核
- **Phase 2（Roadmap P0）**：接入真实视觉审核（CLIP + VLM 逐项核查）
- **Phase 3（Roadmap P1）**：三轮驳回原因回写，指导下一轮搜索词重塑

---

## 留档规范（以后每条建议都按此格式追加）

```markdown
## A## · <一句话摘要>

- **提出时间**：YYYY-MM-DD HH:MM
- **提出人**：Ethan / 其他
- **状态**：⏳ 待评估 / ✅ 已采纳 / ❌ 未采纳 / ⏸ 延期
- **相关产出**：
  - 文件 / 文档路径

### 原始需求（原文保留）

> 用户原话，完整粘贴，不做编辑。

### 落地解读（如已采纳）

| 需求要点 | 落地决策 |
|---|---|
| ... | ... |

### 未采纳原因（如未采纳）

说明理由，保留讨论记录。
```
